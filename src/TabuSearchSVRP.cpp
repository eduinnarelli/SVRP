#include "TabuSearchSVRP.h"

// Etapa 1: Inicialização
TabuSearchSVRP::TabuSearchSVRP(Graph inst, int numVehicles) {

    int h = min(inst.numberVertices - 1, 10);

    // Computar os h vizinhos mais próximos de cada vértice
    for (int i = 0; i < inst.numberVertices; i++) {

        vector<int> closest(h);



    }




}
